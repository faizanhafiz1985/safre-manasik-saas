[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma generate && (npx prisma db push --accept-data-loss || echo 'Warning: prisma db push failed, continuing...') && node src/server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
